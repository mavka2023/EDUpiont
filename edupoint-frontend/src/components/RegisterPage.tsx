import React from 'react';
import { Typography } from '@mui/material';

const RegisterPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Register</Typography>
      <p>Mocked Registration Page</p>
    </div>
  );
};

export default RegisterPage;
